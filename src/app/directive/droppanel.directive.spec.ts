import { DroppanelDirective } from './droppanel.directive';

describe('DroppanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DroppanelDirective();
    expect(directive).toBeTruthy();
  });
});
