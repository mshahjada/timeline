import { TripDirective } from './trip.directive';

describe('TripDirective', () => {
  it('should create an instance', () => {
    const directive = new TripDirective();
    expect(directive).toBeTruthy();
  });
});
