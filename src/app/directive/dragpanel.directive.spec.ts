import { DragpanelDirective } from './dragpanel.directive';

describe('DragpanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DragpanelDirective();
    expect(directive).toBeTruthy();
  });
});
