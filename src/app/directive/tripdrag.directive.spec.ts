import { TripdragDirective } from './tripdrag.directive';

describe('TripdragDirective', () => {
  it('should create an instance', () => {
    const directive = new TripdragDirective();
    expect(directive).toBeTruthy();
  });
});
