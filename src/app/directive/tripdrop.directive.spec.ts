import { TripdropDirective } from './tripdrop.directive';

describe('TripdropDirective', () => {
  it('should create an instance', () => {
    const directive = new TripdropDirective();
    expect(directive).toBeTruthy();
  });
});
